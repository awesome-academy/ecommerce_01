<%= t ".title_hello", email: @resource.email %>

<p><%= t ".notify_locked_account" %></p>

<p><%= t ".title_unlock" %></p>

<p><%= link_to t(".title_link_unlock"), unlock_url(@resource, unlock_token: @token) %></p>
